apiVersion: v1
kind: Service
metadata:
  name: elastic-agent
  namespace: kube-system
spec:
  selector:
    app.kubernetes.io/name: elastic-agent
  ports:
    - protocol: TCP
      port: 9201
      targetPort: 9201
